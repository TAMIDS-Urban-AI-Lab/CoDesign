/**
 * Re-export of React Native's useColorScheme hook for native platforms.
 * This hook provides access to the user's preferred color scheme (light/dark).
 */

export { useColorScheme } from 'react-native';
